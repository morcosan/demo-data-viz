import { Meta } from '@storybook/addon-docs/blocks'
import { CLASS_PREFIX, TOKENS } from '@ds/core'
import { DocsCanvas, DocsToken, DocsTokenCodeGrid, DocsTokenGrid } from '@ds/docs/core'
import { kebabCase } from 'lodash-es'
import { Fragment } from 'react'

export const tokens = Object.entries(TOKENS.SURFACE)

export const Surface = ({ title, children, className }) => (
  <div
    className={cx(
      'min-h-md-0 relative h-fit w-full overflow-visible',
      'py-xs-7 px-xs-9 gap-xs-5 flex flex-col justify-center',
      'text-size-sm truncate',
      className,
    )}
  >
    {title && <div>{title}</div>}
    {children}
  </div>
)

export const TokenValue = ({ value }) => {
  return <DocsToken value={value} type={typeof value === 'string' && value.startsWith('oklch') ? 'color' : undefined} />
}

export const gridClass = cx('gap-x-xs-4 gap-y-xs-4 my-xs-4 grid w-max grid-cols-[auto_1fr] items-center')

{/* ---------------------------------------------------------------------------------------------------------------- */}

<Meta title="Composite tokens / Surface" />

# Surface tokens

<DocsCanvas className="p-sm-0 mb-sm-3 flex flex-col">
  {/* BASIC SURFACES */}
  <div className="gap-xs-9 ds-surface-page min-h-lg-9 grid lg:grid-cols-3">
    <Surface title="page" className="ds-surface-page">
      <Surface title="region" className="ds-surface-region">
        <Surface title="card" className="ds-surface-card">
          <Surface title="popup" className="ds-surface-popup" />
        </Surface>
      </Surface>
    </Surface>
    <Surface>
      <Surface title="card" className="ds-surface-card mt-xs-5">
        <Surface title="popup" className="ds-surface-popup" />
        <Surface title="popup" className="ds-surface-popup translate-y-sm-6" />
      </Surface>
    </Surface>
    <Surface>
      <Surface title="region" className="ds-surface-region">
        <Surface title="popup" className="ds-surface-popup mt-xs-5" />
      </Surface>
      <Surface title="popup" className="ds-surface-popup mt-xs-5" />
    </Surface>
  </div>
  {/* CONTROLS */}
  <Surface className="gap-sm-2! mt-xs-7 flex flex-row! flex-wrap">
    <div className="w-lg-9 gap-xs-3 flex flex-col">
      <Surface title="button-primary" className="ds-surface-button-primary" />
      <Surface title="button-primary-hovered" className="ds-surface-button-primary-hovered" />
    </div>
    <div className="w-lg-9 gap-xs-3 flex flex-col">
      <Surface title="button-secondary" className="ds-surface-button-secondary" />
      <Surface title="button-secondary-hovered" className="ds-surface-button-secondary-hovered" />
    </div>
    <div className="w-lg-9 gap-xs-3 flex flex-col">
      <Surface title="button-default" className="ds-surface-button-default" />
      <Surface title="button-default-hovered" className="ds-surface-button-default-hovered" />
    </div>
    <div className="w-lg-9 gap-xs-3 flex flex-col">
      <Surface title="button-optional" className="ds-surface-button-optional" />
      <Surface title="button-optional-hovered" className="ds-surface-button-optional-hovered" />
    </div>
    <div className="w-lg-9 gap-xs-3 flex flex-col">
      <Surface title="button-danger" className="ds-surface-button-danger" />
      <Surface title="button-danger-hovered" className="ds-surface-button-danger-hovered" />
    </div>
    <div className="w-lg-9 gap-xs-3 flex flex-col">
      <Surface title="button-caution" className="ds-surface-button-caution" />
      <Surface title="button-caution-hovered" className="ds-surface-button-caution-hovered" />
    </div>
    <div className="w-lg-9 gap-xs-3 flex flex-col">
      <Surface title="button-selected" className="ds-surface-button-selected" />
      <Surface title="button-selected-hovered" className="ds-surface-button-selected-hovered" />
    </div>
    <div className="w-lg-9 gap-xs-3 flex flex-col">
      <Surface title="card" className="ds-surface-card" />
      <Surface title="card-hovered" className="ds-surface-card-hovered" />
    </div>
  </Surface>
</DocsCanvas>

<table className="docs">
  <thead>
    <tr>
      <th className="min-w-md-5">Token</th>
      <th>Reference</th>
      <th className="w-full">Value</th>
      <th>Coding</th>
    </tr>
  </thead>
  <tbody>
    {tokens.map(([name, token]) => (
      <Fragment key={name}>
        <tr>
          <td colSpan={4} className="pt-sm-0! pb-0!">
            <pre>{name}</pre>
          </td>
        </tr>
        <tr className="border-0! [&>td]:align-top!">
          <td></td>
          <td>
            <div className={gridClass}>
              {Object.entries(token.ref).map(([key, value]) => (
                <Fragment key={key}>
                  <pre className="text-color-text-subtle text-size-sm">{kebabCase(key)}:</pre>
                  {typeof value === 'object' && !Array.isArray(value) ? (
                    <DocsTokenGrid
                      lightSlot={<DocsToken value={value.light} />}
                      darkSlot={<DocsToken value={value.dark} />}
                      large={Array.isArray(value.light) && Array.isArray(value.dark)}
                    />
                  ) : (
                    <DocsToken value={value} />
                  )}
                </Fragment>
              ))}
            </div>
          </td>
          <td>
            <div className={gridClass}>
              {Object.entries(token.value).map(([key, value]) => (
                <Fragment key={key}>
                  <pre className="text-color-text-subtle text-size-sm">{kebabCase(key)}:</pre>
                  {typeof value === 'object' && !Array.isArray(value) ? (
                    <DocsTokenGrid
                      lightSlot={<TokenValue value={value.light} />}
                      darkSlot={<TokenValue value={value.dark} />}
                      large={Array.isArray(value.light) && Array.isArray(value.dark)}
                    />
                  ) : (
                    <DocsToken value={value} />
                  )}
                </Fragment>
              ))}
            </div>
          </td>
          <td>
            <DocsTokenCodeGrid
              tsVar={`tokens.color['${name}']`}
              classVar={`${CLASS_PREFIX.SURFACE}${name}`}
              tsSize="w-lg-3"
              classSize="w-lg-3"
              delay={1000}
              vertical
            />
          </td>
        </tr>
      </Fragment>
    ))}
  </tbody>
</table>
