import { CSS_PREFIX, TOKENS } from '@ds/core'
import { DocsTokenCoding, DocsTokenThemeGrid } from '@ds/docs/core'
import { Meta } from '@storybook/addon-docs/blocks'

{/* ---------------------------------------------------------------------------------------------------------------- */}

<Meta title="Atomic tokens / Shadow" />

# Shadow tokens

<table className="docs">
  <thead>
    <tr>
      <th>Token</th>
      <th>Value</th>
      <th className="w-full">Preview</th>
      <th>Coding</th>
    </tr>
  </thead>
  <tbody>
    {Object.entries(TOKENS.SHADOW).map(([name, token]) => (
      <tr key={name}>
        <td>
          <pre>{name}</pre>
        </td>
        <td>
          <DocsTokenThemeGrid lightSlot={<code>{token.value.light}</code>} darkSlot={<code>{token.value.dark}</code>} />
        </td>
        <td>
          <div className="gap-xs-7 flex items-center">
            <div className="h-sm-8 w-sm-8 bg-color-bg-sheet" style={{ boxShadow: String(token.value.light) }} />
            <div className="h-sm-8 w-sm-8 bg-color-bg-sheet" style={{ boxShadow: String(token.value.dark) }} />
          </div>
        </td>
        <td>
          <DocsTokenCoding
            tsVar={`tokens.shadow['${name}']`}
            tsSize="w-lg-2"
            twVars={[`shadow-${name}`]}
            twSize="w-lg-0"
            cssVar={`${CSS_PREFIX.SHADOW}${name}`}
            cssSize="w-lg-2"
          />
        </td>
      </tr>
    ))}
  </tbody>
</table>
