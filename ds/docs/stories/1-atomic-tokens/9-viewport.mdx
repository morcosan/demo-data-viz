import { CSS_PREFIX, DesktopSvg, LaptopSvg, PhoneSvg, TabletSvg, TOKENS, WidescreenSvg } from '@ds/core'
import { DocsTokenCodeGrid } from '@ds/docs/core'
import { Meta } from '@storybook/addon-docs/blocks'

export const breakpointMap = {
  xs: { icon: <PhoneSvg className="w-xs-9" />, title: 'Phone' },
  sm: { icon: <TabletSvg className="w-sm-0" />, title: 'Tablet' },
  md: { icon: <TabletSvg className="w-sm-0 -rotate-90" />, title: 'Tablet' },
  lg: { icon: <LaptopSvg className="w-sm-4" />, title: 'Laptop' },
  xl: { icon: <DesktopSvg className="w-sm-4" />, title: 'Desktop' },
  xxl: { icon: <WidescreenSvg className="w-sm-5" />, title: 'Widescreen' },
}

{/* ---------------------------------------------------------------------------------------------------------------- */}

<Meta title="Atomic tokens / Viewport" />

# Viewport tokens

## Breakpoint tokens

<table className="docs">
  <thead>
    <tr>
      <th>Token</th>
      <th className="w-full">Value</th>
      <th>Coding</th>
    </tr>
  </thead>
  <tbody>
    {Object.entries(TOKENS.BREAKPOINT).map(([name, token]) => (
      <tr key={name}>
        <td>
          <div className="gap-xs-4 text-color-text-subtle flex items-center">
            <div className="flex-center w-sm-5">{breakpointMap[name].icon}</div>
            <div className="flex flex-col leading-1 whitespace-nowrap">
              <pre>{name}</pre>
              <div className="text-size-xs text-color-text-subtle">{breakpointMap[name].title}</div>
            </div>
          </div>
        </td>
        <td>
          <code>{token.value}</code>
        </td>
        <td>
          <DocsTokenCodeGrid
            tsVar={`tokens.breakpoint['${name}']`}
            tsSize="w-lg-3"
            twVars={[`${name}:___`]}
            twSize="w-md-3"
            cssVar={`${CSS_PREFIX.BREAKPOINT}${name}`}
            cssSize="w-lg-4"
          />
        </td>
      </tr>
    ))}
  </tbody>
</table>
